import { createApp } from 'vue';
import App from './App.vue';
//import AApp from './App2.vue'; // Assume you have another Vue component

// Mount the first app
createApp(App).mount('#app');

// Mount the second app
//createApp(AnotherApp).mount('#another-app');
